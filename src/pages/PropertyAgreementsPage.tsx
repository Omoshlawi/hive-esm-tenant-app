import React, { FC } from "react";
import { PropsWithLaunchWorkspace } from "../types";

const PropertyAgreementsPage:FC<PropsWithLaunchWorkspace> = ({launchWorkspace}) => {
  return <div>PropertyAgreementsPage</div>;
};

export default PropertyAgreementsPage;
